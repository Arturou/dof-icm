# Runtime dependencies for the DOF-ICM web UI (web/).
# This mirrors the upstream dof-rag human_eval stack (MIT), minus the
# corpus/vector dependencies: no sqlite-vec, no sentence-transformers, no
# marker-pdf. Python 3.13+.
#
# Install into a dedicated venv (see web/README.md) or reuse the legacy env
# (../dof-rag/.venv, created with `cd ../dof-rag && uv sync`):
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt
air>=0.48.1
uvicorn>=0.35,<1
starlette>=0.47
itsdangerous>=2.2,<3
python-multipart>=0.0.20,<1
markdown-it-py>=3.0.0,<4
nh3>=0.3.6,<1
openai>=2.50.0

# Clerk auth is optional: required only when DOF_AUTH_BACKEND=clerk.
# The default "local" single-admin mode does not import these.
airclerk>=0.2.0
